<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.RentifyX" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Remove the splash screen background -->
        <item name="android:windowBackground">@android:color/white</item>
        <!-- Optionally remove the status bar background -->
        <item name="android:statusBarColor">@android:color/white</item>

        <item name="android:windowSplashScreenAnimatedIcon">@drawable/empty</item>
    </style>
</resources>